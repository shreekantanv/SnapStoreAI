# -----------------------------------------------------------------------------
# Environment Variables for AI Tool Store Backend
# -----------------------------------------------------------------------------
#
# Create a .env file in this directory and copy these variables into it.
# These are used by the Firebase emulator and for deployment.
#
# IMPORTANT: DO NOT commit the actual .env file to version control.
#

# --- Firebase Project ---
# These are typically not needed when deployed, as the Admin SDK auto-detects them,
# but they can be useful for local testing with the emulator.
# GCLOUD_PROJECT="your-firebase-project-id"

# --- Third-Party AI Model API Keys ---
# Add the API keys for the services you will be calling from the /runTool function.
OPENAI_API_KEY="sk-..."
ANTHROPIC_API_KEY="..."
GEMINI_API_KEY="..."
GROK_API_KEY="..."

# --- Payment Provider Webhook Secrets ---
# These are crucial for verifying that webhooks are legitimate.
# Get these from your Stripe and other payment provider dashboards.
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
